{
  "name": "dalledress-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "yarn --cwd frontend install && wails build -clean",
    "dev": "wails dev",
    "start": "yarn dev",
    "lint": "golangci-lint run ./... && yarn --cwd frontend lint",
    "type-check": "yarn --cwd frontend type-check",
    "test": "go test ./..."
  }
}

{
    "scripts": {
        "dev": "wails dev --debounce 2000 2>&1 | grep -v AssetHandler | grep -v base | grep -v types"
    },
    "name": "trueblocks-dalledress",
    "version": "1.0.0",
    "license": "GPL-3.0"
}
